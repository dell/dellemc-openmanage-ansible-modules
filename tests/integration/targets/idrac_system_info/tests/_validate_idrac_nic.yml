---
- name: Fetch manager details
  ansible.builtin.uri:
    url: "{{ manager_uri }}"
    user: "{{ idrac_user }}"
    password: "{{ idrac_password }}"
    method: GET
    force_basic_auth: true
    validate_certs: false
    body_format: json
    return_content: true
    status_code: 200
  register: uri_manager_data

- name: Fetch idrac_nic details
  ansible.builtin.uri:
    url: "{{ manager_idrac_attributs_uri }}"
    user: "{{ idrac_user }}"
    password: "{{ idrac_password }}"
    method: GET
    force_basic_auth: true
    validate_certs: false
    body_format: json
    return_content: true
    status_code: 200
  register: uri_data

- name: Structuring idrac nic details
  ansible.builtin.command:
    cmd: "python _get_idrac_nic_details.py {{ a }} {{ b }}"
    chdir: "{{ playbook_dir }}/targets/idrac_system_info/tests"
  environment:
    PYTHONPATH: "{{ lookup('env', 'ANSIBLE_COLLECTIONS_PATH') }}"
  register: idrac_nic_updated_dict
  vars:
    a: "{{ uri_manager_data.json | to_json | quote }}"
    b: "{{ uri_data.json | to_json | quote }}"
  when:
    - uri_data.status == 200
    - uri_manager_data.status == 200

- name: Output conversion to list
  ansible.builtin.set_fact:
    idrac_nic_updated_dict: "{{ idrac_nic_updated_dict.stdout | from_yaml }}"
  when:
    - uri_data.status == 200
    - uri_manager_data.status == 200

- name: Get idrac NIC data
  ansible.builtin.set_fact:
    res_idrac_nic:
      "{{ result.system_info.iDRACNIC }}"

- name: Output conversion to list change none
  ansible.builtin.set_fact:
    result_idrac_nic_updated_dict:
      "{{ res_idrac_nic | regex_replace('null', '\"None\"') | from_yaml}}"

- name: Comparing values inside list for memory
  ansible.builtin.include_tasks:
    file: _compare_diff.yml
  vars:
    _before: "{{ item.0 }}"
    _after: "{{ item.1 }}"
  when:
    - uri_data.status == 200
    - uri_manager_data.status == 200
  loop: "{{ result_idrac_nic_updated_dict | zip(idrac_nic_updated_dict) }}"
