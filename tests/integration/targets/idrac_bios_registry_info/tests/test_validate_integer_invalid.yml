---
# Integration test for integer out of range
# Test Case: test_validate_integer_invalid
# Gherkin Scenario: Integer out of range (AC-003)
# FR Reference: FR-2.2
# This test verifies that the module correctly detects an integer value
# outside the defined LowerBound and UpperBound from the registry.
- name: Validate integer out of range
  dellemc.openmanage.idrac_bios_registry_info:
    idrac_ip: "{{ idrac_ip }}"
    idrac_user: "{{ idrac_user }}"
    idrac_password: "{{ idrac_password }}"
    idrac_port: "{{ idrac_port | default(443) }}"
    validate_certs: "{{ validate_certs | default(true) }}"
    validate: true
    attributes:
      NvmeofSubsys2Timeout: "50000"
  register: validation_result

- name: Verify module succeeded
  assert:
    that:
      - validation_result is not failed
      - validation_result is succeeded

- name: Verify validation result is false
  assert:
    that:
      - validation_result.valid is defined
      - validation_result.valid is false

- name: Verify validation results structure
  assert:
    that:
      - validation_result.validation_results is defined
      - validation_result.validation_results is iterable

- name: Verify NvmeofSubsys2Timeout validation status
  assert:
    that:
      - validation_result.validation_results | selectattr('attribute', 'equalto', 'NvmeofSubsys2Timeout') | map(attribute='status') | list | first == "invalid"

- name: Verify error reason
  assert:
    that:
      - validation_result.validation_results | selectattr('attribute', 'equalto', 'NvmeofSubsys2Timeout') | map(attribute='reason') | list | first is search("exceeds maximum")

- name: Verify validation counts
  assert:
    that:
      - validation_result.valid_count is defined
      - validation_result.valid_count == 0
      - validation_result.invalid_count is defined
      - validation_result.invalid_count == 1

- name: Display validation summary
  debug:
    msg:
      - "Valid: {{ validation_result.valid }}"
      - "Valid Count: {{ validation_result.valid_count }}"
      - "Invalid Count: {{ validation_result.invalid_count }}"
      - "Validation Results: {{ validation_result.validation_results }}"
