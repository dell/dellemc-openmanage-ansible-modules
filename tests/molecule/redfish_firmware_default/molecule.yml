---
provisioner:
  name: ansible
  playbooks:
    prepare: ../resources/redfish_firmware/prepare.yml
    cleanup: ../resources/redfish_firmware/cleanup.yml
scenario:
  test_sequence:
    - cleanup
    - destroy
    - syntax
    - create
    - converge
    - destroy
