---
provisioner:
  name: ansible
  playbooks:
    prepare: ../helper/common/power_on_idrac_and_clear_all_job.yaml
    cleanup: ../helper/idrac_user/cleanup.yml
scenario:
  test_sequence:
    - cleanup
    - converge
    - destroy
