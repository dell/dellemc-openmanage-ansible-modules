fixes:
   - "/ansible_collections/dellemc/openmanage/::"
coverage:
   range: "75...90"
   status:
      patch:
         default:
            target: 90%
            threshold: 0%
            if_ci_failed: error
